# SYMBOL Language Rules

1. **No words for operators.** All operators are symbols, not words like `first` or `max`.

2. **Operators are strictly monadic or dyadic.** Never both. No overloading by arity.
